[project]
name = "lab02-cnn"
version = "1.0.0"
description = "Lab 2: convolutional neural networks from scratch"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0,<3",
    "matplotlib>=3.8",
    "pytest>=8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--tb=no -q"
